# Configurações da API Pravaler
# Copie este arquivo para .env e ajuste as configurações

# Banco de dados
# SQLite (desenvolvimento)
DATABASE_URL=sqlite:///./pravaler.db

# PostgreSQL (produção)
# DATABASE_URL=postgresql+psycopg2://usuario:senha@localhost:5432/pravaler

# Ambiente
APP_ENV=dev

# Segurança (em produção, use uma chave fixa e segura)
# Para gerar uma chave: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your_secret_key_here

# Configurações JWT (opcional - valores padrão)
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# ALGORITHM=HS256
